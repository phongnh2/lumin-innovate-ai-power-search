import styled from '@emotion/styled';

import { Colors } from '@/ui';

export const EmailPreserve = styled.p`
  display: block;
  margin: 4px 0 0;
  color: ${Colors.SECONDARY_50};
  font-size: 12px;
  line-height: 16px;
  font-weight: 400;
`;
