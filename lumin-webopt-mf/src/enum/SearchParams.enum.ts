export enum SearchParams {
  TemplateSlug = "template_link",
  IssueSlug = "issue",

  /**
   * @description use for redirecting to Lumin Web or Lumin Editor
   * @example `app_target=web` or `app_target=editor`
   */
  AppTarget = "app_target",
}

export enum ReportIssues {
  WrongLocation = "wrong-location",
}
