const FILE_OPERATIONS = {
  READ: 'read',
  WRITE: 'write',
  OPEN: 'open',
};

const OS_PLATFORM = {
  DARWIN: 'darwin',
  WIN32: 'win32',
  LINUX: 'linux',
};

const ENVIRONMENT = {
  DEVELOPMENT: 'development',
  PRODUCTION: 'production',
};

module.exports = {
  FILE_OPERATIONS,
  OS_PLATFORM,
  ENVIRONMENT,
};
