/**
 * Protocol constants for Lumin PDF Electron app
 */
const PROTOCOL_SCHEME = 'luminpdf://';
const PROTOCOL_RETRY_CONNECTION = `${PROTOCOL_SCHEME}retry-connection`;

module.exports = {
  PROTOCOL_SCHEME,
  PROTOCOL_RETRY_CONNECTION,
};
