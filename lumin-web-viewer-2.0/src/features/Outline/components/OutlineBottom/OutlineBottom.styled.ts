import styled from "styled-components";

import { spacings } from 'constants/styles/editor';

export const OutlineBottomContainer = styled.div`
  .MuiDivider-root {
    margin: 0;
  }
`;

export const OutlineBottomWrapper = styled.div`
  padding: ${spacings.le_gap_1}px ${spacings.le_gap_2}px;
`;
