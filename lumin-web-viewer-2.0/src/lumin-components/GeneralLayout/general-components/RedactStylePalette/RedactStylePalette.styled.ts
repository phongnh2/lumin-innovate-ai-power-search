import styled from 'styled-components';

import ColorPalette from '@new-ui/general-components/ColorPalette';
import { spacings } from 'constants/styles/editor';

export const CustomColorPalette = styled(ColorPalette)`
  padding-left: ${spacings.le_gap_0_5}px;
`;
