export enum NavigationTypes {
  Link = 'link',
  SubMenu = 'submenu',
  Toggle = 'toggle',
}

export enum SubMenuTypes {
  Documents = 'documents',
  Settings = 'settings',
  Signs = 'signs',
  AgreementGen = 'agreement_gen',
  Templates = 'templates',
}

export enum ModalTypes {
  Invite_Members = 'invite_members',
}

export enum ActiveMenuItemTypes {
  Default = 'default',
  Documents = 'documents',
  Settings = 'settings',
  Signs = 'signs',
  Templates = 'templates',
}
