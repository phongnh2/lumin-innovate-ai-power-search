export declare function saveHubspotProperties({ key, value }: { key: string; value: string }): Promise<void>;

export declare function ratedApp({ ratedScore }: { ratedScore: number }): Promise<void>;
